;; Config baked into the Docker image by ./Dockerfile. Mount your own
;; config.edn + zones-dir over these paths (or rebuild the image) to serve
;; real zones instead of the bundled example.
{:host "0.0.0.0" :port 53 :zones-dir "/app/zones"}
