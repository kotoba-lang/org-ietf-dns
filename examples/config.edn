;; Sample config for nameserver.main. Copy, edit :zones-dir to a real
;; absolute path, drop :port to 1053 for unprivileged local testing (53
;; needs root/CAP_NET_BIND_SERVICE -- see README "Running for real").
{:host "127.0.0.1"
 :port 1053
 :zones-dir "examples/zones"
 :custom-tld {:suffixes ["hogehoge."]
              :gateway-host "ipns.dweb.link"}}
